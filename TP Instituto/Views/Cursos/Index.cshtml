@model IEnumerable<TP_Instituto.Models.Cursos>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Legajo_Alumn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre_Alumn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Apellido_Alumn)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Legajo_Alumn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_Alumn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Apellido_Alumn)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCurso }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCurso }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCurso })
        </td>
    </tr>
}

</table>
